<div class="row">
    <div class="col-md-10">
        <h1>
            <%= @library.name %> | <%= number_to_human(@library.average_rating) %> <span class="glyphicon glyphicon-star"></span>
        </h1>
        <%= content_tag(:h4, ("version: #{@library.version}" if @library.version)) %>
        <h4>
            <%= pluralize(@library.vote_count, "vote") %> | <%= pluralize(@library.comment_count, "comment") %>
        </h4>
        <h3><b>Description:</b></h3>
        <p><%= @library.description %></p>
    </div>
    <div class="col-md-2">
        <h3>Details: </h3>
        <p>
            <%= link_to "Documentation", @library.documentation_url %><br/> 
            <%= link_to "Project page", @library.library_url %><br/> 
            Language: <%= link_to(@library.language.name, language_path(@library.language)) %><br/> 
            Framework: <%= link_to(@library.framework.name, framework_path(@library.framework)) %>
        </p>
        <% if user_signed_in? %>
        <%= link_to "Edit", edit_library_path(@library), class: "btn btn-primary" %>
        <%= link_to "Delete", library_path(@library), method: :delete, class: "btn btn-danger" %>
        <% end %>
    </div>
</div>
<hr>
<% if @library.comment_count > 0 %>
<h2>Comments</h2>
    <ul>
    <%= render partial: 'reviews/comment_li', collection: @library.comments, as: :comment %>
    </ul>
<% end %>

<% if user_signed_in? %>
<h3>Create Comment</h3>
<%= render 'reviews/inline_form' %>
<% end %>